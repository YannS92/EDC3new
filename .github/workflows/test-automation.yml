name: DigitalBank - Docker Container Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Build images
        run: docker compose build

      - name: Run tests related containers only
        run: |
          docker compose run --rm tests

      - name: Stop containers
        if: always()
        run: docker compose down -v

      - name: Upload HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: digitalbank-html-report
          path: digitalbank-automation/reports/report.html
          retention-days: 30